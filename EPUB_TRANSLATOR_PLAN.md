# EPUB 翻译工具 - 产品规划文档

## 📋 产品概述

### 产品定位
一个基于Web的EPUB电子书智能翻译工具，支持多种AI翻译引擎，保持原书排版结构的同时实现高质量翻译。

### 目标用户
- 外语学习者：需要阅读外文原版书籍
- 研究人员：需要快速理解外文文献
- 阅读爱好者：想阅读未翻译的外文作品
- 翻译工作者：需要初步翻译稿进行精修

### 核心价值主张
1. **保留原味**：完整保留EPUB结构、排版、图片、目录
2. **灵活选择**：支持多种AI翻译引擎（Gemini、OpenAI、Claude）
3. **即时预览**：边翻译边预览，所见即所得
4. **简单易用**：无需安装，浏览器打开即用

---

## 🎯 核心功能模块

### 1. 文件管理模块
**功能清单：**
- ✅ EPUB文件上传（拖拽上传 + 点击选择）
- ✅ 文件格式验证（.epub）
- ✅ 文件大小限制（建议50MB以内）
- ✅ 上传进度显示
- ✅ 文件信息展示（书名、作者、章节数、字数）

**技术要点：**
- 使用 JSZip 解析EPUB文件（EPUB本质是ZIP压缩包）
- 解析 META-INF/container.xml 获取目录
- 解析 content.opf 获取书籍元数据和章节列表

---

### 2. EPUB解析与展示模块
**功能清单：**
- ✅ EPUB结构解析（目录、章节、样式）
- ✅ 章节导航（左侧目录树）
- ✅ 原文阅读视图（中间区域）
- ✅ 译文对照视图（可切换）
- ✅ 保留原有排版（CSS样式、字体、图片）
- ✅ 响应式设计（适配移动端）

**UI布局：**
```
┌─────────────────────────────────────────────────┐
│  [EPUB Translator] 🌐                [设置]     │
├──────────┬──────────────────────────────────────┤
│          │  Chapter 1: Introduction             │
│  目录    │  ────────────────────────────────    │
│  📖      │                                       │
│  ├ Ch1   │  原文段落 1...                        │
│  ├ Ch2   │  [译文段落 1...] ← 翻译后显示        │
│  ├ Ch3   │                                       │
│  └ Ch4   │  原文段落 2...                        │
│          │  [正在翻译中... ⏳]                   │
│          │                                       │
│  [开始翻译]│                                     │
│  [下载]   │                                       │
└──────────┴──────────────────────────────────────┘
```

**技术要点：**
- 使用 epub.js 或自己实现轻量级EPUB渲染引擎
- HTML解析：保留所有标签和属性
- 样式隔离：使用iframe或Shadow DOM
- 图片处理：保留原有图片引用

---

### 3. 翻译引擎模块
**功能清单：**
- ✅ 支持多种AI引擎：
  - OpenAI (GPT-4o, GPT-4o-mini)
  - Anthropic Claude (Sonnet, Haiku)
  - Google Gemini (1.5 Pro, 1.5 Flash)
- ✅ API Key管理（本地存储，加密保存）
- ✅ 翻译参数配置：
  - 源语言 / 目标语言
  - 翻译风格（直译 / 意译 / 文学性）
  - 专业领域（通用 / 技术 / 文学 / 学术）
- ✅ 批量翻译（按段落分批）
- ✅ 进度追踪与断点续传
- ✅ 成本估算（根据字数和模型）

**翻译策略：**
```javascript
// 分段翻译策略
1. 按段落切分（<p>标签）
2. 合并短段落（<100字）避免过多API调用
3. 保留HTML标签不翻译
4. 批量发送（每批5-10段）
5. 错误重试机制（最多3次）
6. 速率限制（避免触发API限流）
```

**Prompt设计：**
```
你是专业的文学翻译专家。请将以下英文段落翻译成中文，要求：
1. 保持原文风格和语气
2. 准确传达原意
3. 符合中文阅读习惯
4. 不要添加任何额外解释

原文：
{source_text}

请只返回翻译结果，不要包含其他内容。
```

---

### 4. API配置管理模块
**功能清单：**
- ✅ 可视化配置界面
- ✅ 多引擎API Key管理
- ✅ 测试连接功能
- ✅ 本地加密存储（不上传服务器）
- ✅ 快速切换引擎

**配置UI：**
```
┌─────────────────────────────────────┐
│  ⚙️  翻译引擎配置                    │
├─────────────────────────────────────┤
│  选择引擎：                          │
│  ○ OpenAI                           │
│    API Key: [******************]    │
│    模型: [GPT-4o ▼]                 │
│    [测试连接]                        │
│                                     │
│  ○ Anthropic Claude                 │
│    API Key: [******************]    │
│    模型: [Claude Sonnet ▼]          │
│    [测试连接]                        │
│                                     │
│  ○ Google Gemini                    │
│    API Key: [******************]    │
│    模型: [Gemini 1.5 Pro ▼]         │
│    [测试连接]                        │
│                                     │
│  翻译设置：                          │
│  源语言: [English ▼]                │
│  目标语言: [简体中文 ▼]              │
│  翻译风格: [意译 ▼]                 │
│                                     │
│  [保存设置]  [取消]                  │
└─────────────────────────────────────┘
```

---

### 5. 翻译控制模块
**功能清单：**
- ✅ 开始/暂停/继续翻译
- ✅ 取消翻译
- ✅ 实时进度显示（百分比、剩余时间）
- ✅ 章节选择性翻译（只翻译指定章节）
- ✅ 翻译历史记录
- ✅ 错误日志与重试

**进度显示：**
```
┌─────────────────────────────────────┐
│  正在翻译：第3章 / 共15章           │
│  ████████░░░░░░░░  45%              │
│                                     │
│  已翻译：450段 / 共1000段            │
│  预计剩余时间：5分钟                 │
│  已使用Token：~125,000              │
│  预计成本：$0.25                    │
│                                     │
│  [暂停] [取消]                       │
└─────────────────────────────────────┘
```

---

### 6. EPUB生成与下载模块
**功能清单：**
- ✅ 生成新的EPUB文件
- ✅ 保留原有结构（目录、metadata、图片）
- ✅ 三种输出模式：
  - 纯译文版
  - 中英对照版（原文+译文）
  - 双语版（段落对照）
- ✅ 自定义文件名
- ✅ 下载进度提示

**EPUB生成流程：**
```
1. 克隆原EPUB结构
2. 替换HTML内容（翻译后的文本）
3. 更新metadata（标题加"（中文版）"）
4. 重新打包成ZIP（改名.epub）
5. 触发浏览器下载
```

---

## 🏗️ 技术架构

### 架构图
```
┌─────────────────────────────────────────────────┐
│              前端 Web Application                │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 文件上传  │  │ EPUB渲染 │  │ 翻译控制 │      │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘      │
│       │             │              │            │
│  ┌────▼─────────────▼──────────────▼─────┐     │
│  │      核心翻译引擎 (Translation Core)    │     │
│  │  - 文本分段  - API调用  - 进度管理     │     │
│  └────┬──────────┬───────────┬────────────┘     │
│       │          │           │                  │
└───────┼──────────┼───────────┼──────────────────┘
        │          │           │
        ▼          ▼           ▼
   ┌────────┐ ┌────────┐ ┌────────┐
   │OpenAI  │ │Claude  │ │Gemini  │
   │  API   │ │  API   │ │  API   │
   └────────┘ └────────┘ └────────┘
```

### 技术栈选择

#### 前端框架
**方案A：纯原生实现（推荐用于MVP）**
- ✅ 优势：无依赖、轻量、快速启动
- ✅ 适合：快速验证、简单部署
- ❌ 劣势：复杂状态管理较困难

**方案B：React + TypeScript**
- ✅ 优势：组件化、类型安全、生态丰富
- ✅ 适合：长期维护、复杂交互
- ❌ 劣势：需要构建工具、学习成本

**建议：先用原生实现MVP，后期可迁移到React**

#### 核心依赖库
```json
{
  "jszip": "^3.10.1",          // EPUB解析（ZIP压缩）
  "epub.js": "^0.3.93",        // EPUB渲染（可选）
  "dompurify": "^3.0.6",       // HTML安全过滤
  "localforage": "^1.10.0"     // 本地存储（IndexedDB）
}
```

#### API调用方案
```javascript
// 统一的API抽象层
class TranslationAPI {
  constructor(provider, apiKey, model) {
    this.provider = provider; // 'openai' | 'anthropic' | 'gemini'
    this.apiKey = apiKey;
    this.model = model;
  }

  async translate(text, options) {
    // 根据provider调用不同API
  }
}
```

---

## 📱 用户体验流程

### 主流程
```
1. 打开网页
   ↓
2. 首次使用 → 配置API Key（引导式设置）
   ↓
3. 上传EPUB文件（拖拽或选择）
   ↓
4. 解析完成 → 展示书籍信息和目录
   ↓
5. 选择翻译设置（引擎、语言、风格）
   ↓
6. 点击"开始翻译" → 实时显示进度
   ↓
7. 翻译完成 → 预览译文
   ↓
8. 选择输出模式 → 下载翻译版EPUB
```

### 关键交互优化
1. **拖拽上传动画**：虚线边框高亮提示
2. **解析加载状态**：骨架屏展示
3. **翻译进度可视化**：进度条 + 实时日志
4. **错误提示友好**：具体错误原因 + 解决建议
5. **快捷键支持**：
   - `Ctrl+O`: 打开文件
   - `Ctrl+S`: 下载翻译
   - `Space`: 暂停/继续

---

## 🎨 UI/UX设计要点

### 设计原则
1. **简洁至上**：主界面只显示必要元素
2. **渐进式引导**：首次使用有引导提示
3. **即时反馈**：所有操作都有立即响应
4. **容错设计**：支持中断恢复、错误重试

### 配色方案
```css
/* 主题色 */
--primary-color: #4F46E5;      /* 靛蓝 - 专业感 */
--secondary-color: #10B981;    /* 绿色 - 成功状态 */
--warning-color: #F59E0B;      /* 橙色 - 警告 */
--error-color: #EF4444;        /* 红色 - 错误 */

/* 背景色 */
--bg-primary: #FFFFFF;
--bg-secondary: #F9FAFB;
--bg-dark: #1F2937;

/* 文字色 */
--text-primary: #111827;
--text-secondary: #6B7280;
```

### 响应式断点
```css
/* 移动端 */
@media (max-width: 768px) {
  /* 堆叠布局，隐藏侧边栏 */
}

/* 平板 */
@media (min-width: 769px) and (max-width: 1024px) {
  /* 紧凑布局 */
}

/* 桌面 */
@media (min-width: 1025px) {
  /* 完整布局 */
}
```

---

## 🚀 实施计划

### MVP版本 (v0.1) - 2-3周
**目标：验证核心功能可行性**

**Week 1：基础架构**
- [ ] 项目初始化（HTML/CSS/JS结构）
- [ ] EPUB上传与解析（JSZip）
- [ ] 基础UI布局（目录 + 阅读区域）
- [ ] EPUB内容展示（HTML渲染）

**Week 2：翻译核心**
- [ ] API配置界面（支持OpenAI）
- [ ] 文本提取与分段算法
- [ ] 翻译API调用（OpenAI集成）
- [ ] 翻译结果插入与展示

**Week 3：生成与优化**
- [ ] EPUB生成功能（纯译文版）
- [ ] 下载功能
- [ ] 进度条显示
- [ ] 基础错误处理

**MVP功能范围：**
- ✅ 单引擎支持（OpenAI）
- ✅ 基础翻译（纯译文）
- ✅ 简单UI
- ❌ 暂不支持：双语对照、多引擎、高级设置

---

### v0.2 版本 - 1-2周
**目标：增强用户体验**

- [ ] 支持多引擎（Claude、Gemini）
- [ ] API Key本地加密存储
- [ ] 翻译进度百分比和预估时间
- [ ] 暂停/继续翻译功能
- [ ] 错误重试机制
- [ ] 双语对照模式

---

### v0.3 版本 - 2周
**目标：完善功能**

- [ ] 章节选择性翻译
- [ ] 翻译历史记录
- [ ] 翻译质量设置（风格、领域）
- [ ] 成本估算功能
- [ ] 响应式设计优化
- [ ] 深色模式支持

---

### v1.0 正式版 - 1-2周
**目标：产品化**

- [ ] 完整的错误处理和用户提示
- [ ] 性能优化（大文件处理）
- [ ] 使用文档和帮助系统
- [ ] 多语言界面（中英文）
- [ ] 用户反馈机制
- [ ] 单元测试覆盖

---

## ⚠️ 技术难点与解决方案

### 1. EPUB结构复杂性
**问题：** EPUB 2.0 vs 3.0 格式差异，嵌套的HTML结构
**解决：**
- 使用成熟的库（JSZip + 自定义解析器）
- 支持主流格式，边缘情况给出友好提示
- 保留原始文件结构，最小化修改

### 2. 大文件处理
**问题：** 大型EPUB（几十MB）可能导致浏览器卡顿
**解决：**
- 分块读取和处理（Web Worker）
- 虚拟滚动技术（只渲染可见章节）
- 显示加载进度，避免假死

### 3. HTML标签保留
**问题：** 翻译时需要保留HTML标签，只翻译文本内容
**解决：**
```javascript
// 提取纯文本，保留标签结构
function extractTextWithPlaceholders(html) {
  const tagRegex = /<[^>]+>/g;
  const tags = [];
  let index = 0;

  const text = html.replace(tagRegex, (tag) => {
    tags.push(tag);
    return `{{TAG_${index++}}}`;
  });

  return { text, tags };
}

// 翻译后恢复标签
function restoreTags(translatedText, tags) {
  return translatedText.replace(/\{\{TAG_(\d+)\}\}/g,
    (match, idx) => tags[parseInt(idx)]
  );
}
```

### 4. API成本控制
**问题：** 一本书可能需要几万Token，成本较高
**解决：**
- 翻译前显示预估成本
- 支持暂停以控制开销
- 提供便宜模型选项（GPT-4o-mini、Haiku）
- 智能合并短段落减少API调用

### 5. API限流与错误
**问题：** API可能有速率限制，或返回错误
**解决：**
- 指数退避重试（Exponential Backoff）
- 批次间延迟（Rate Limiting）
- 保存翻译进度，支持断点续传
- 详细错误日志供用户排查

### 6. 跨域问题
**问题：** 浏览器可能阻止API调用
**解决：**
- 所有API都支持CORS，直接调用
- 或提供可选的代理服务器
- 本地开发使用CORS插件

---

## 💰 商业化考虑（可选）

### 免费版
- ✅ 所有核心功能
- ✅ 用户自备API Key
- ✅ 无文件大小限制
- ❌ 无云存储

### 潜在增值方向（未来）
1. **云端版本**：无需API Key，按翻译字数收费
2. **批量处理**：同时翻译多本书
3. **翻译记忆库**：重复内容不重复翻译
4. **术语库管理**：专有名词一致性
5. **协作功能**：多人审校翻译

---

## 📊 成功指标

### 产品指标
- 📈 EPUB解析成功率 > 95%
- ⚡ 平均翻译速度：1000字/分钟
- 🎯 翻译质量评分 > 4.0/5.0（用户反馈）
- 💾 生成的EPUB可用率 > 98%

### 用户体验指标
- ⏱️ 首次使用完成时间 < 5分钟
- 🔁 7日留存率 > 40%
- ⭐ 用户满意度 > 4.5/5.0

---

## 🛠️ 开发规范

### 代码规范
```javascript
// 文件命名：kebab-case
epub-parser.js
translation-engine.js
api-config.js

// 函数命名：camelCase
function parseEpubStructure() {}
function translateText() {}

// 类命名：PascalCase
class TranslationEngine {}
class EpubGenerator {}

// 常量命名：UPPER_SNAKE_CASE
const MAX_FILE_SIZE = 50 * 1024 * 1024; // 50MB
const API_TIMEOUT = 30000; // 30秒
```

### 注释规范
```javascript
/**
 * 翻译单个段落
 * @param {string} text - 原文文本
 * @param {Object} options - 翻译选项
 * @param {string} options.from - 源语言
 * @param {string} options.to - 目标语言
 * @returns {Promise<string>} 翻译结果
 */
async function translateParagraph(text, options) {
  // 实现...
}
```

---

## 🎯 优先级矩阵

### P0 - 必须有（MVP）
- ✅ EPUB上传与解析
- ✅ 单引擎翻译（OpenAI）
- ✅ 纯译文展示
- ✅ EPUB下载

### P1 - 应该有（v0.2）
- ⭐ 多引擎支持（Claude、Gemini）
- ⭐ 双语对照模式
- ⭐ 翻译进度管理
- ⭐ 错误处理与重试

### P2 - 可以有（v0.3+）
- 📌 章节选择翻译
- 📌 翻译历史
- 📌 深色模式
- 📌 响应式优化

### P3 - 未来考虑
- 💡 翻译记忆库
- 💡 批量处理
- 💡 云端服务

---

## 📝 风险评估

| 风险项 | 可能性 | 影响 | 应对策略 |
|--------|--------|------|----------|
| EPUB格式兼容性问题 | 中 | 高 | 使用成熟库，提供格式验证 |
| API成本超预期 | 高 | 中 | 提前成本估算，支持暂停 |
| 翻译质量不佳 | 中 | 高 | 优化Prompt，支持模型切换 |
| 浏览器性能问题 | 中 | 中 | 分块处理，Web Worker |
| API限流 | 高 | 中 | 速率控制，指数退避 |

---

## 🎓 用户教育

### 新手引导
1. **欢迎页**：介绍工具功能和使用流程
2. **首次配置向导**：逐步引导设置API Key
3. **示例文件**：提供测试用小型EPUB
4. **视频教程**：录制3分钟快速上手视频

### 帮助文档
- 📖 如何获取API Key（各平台详细步骤）
- 📖 翻译质量优化建议
- 📖 常见问题解答（FAQ）
- 📖 故障排除指南

---

## 总结

这是一个**技术可行、用户需求明确、实施路径清晰**的产品。核心优势在于：

1. **纯前端实现** - 无需服务器，降低部署和维护成本
2. **灵活性高** - 用户自选AI引擎，不被单一服务商绑定
3. **隐私友好** - API Key本地存储，文件不上传服务器
4. **渐进式开发** - MVP验证可行性，逐步迭代完善

**建议：先用2-3周快速实现MVP版本，验证核心功能和用户反馈，再决定后续投入。**
