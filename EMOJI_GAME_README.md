# 🎮 表情符号谚语猜谜游戏

一个有趣的猜谜游戏，使用 Claude AI 将中国谚语翻译成表情符号，玩家需要根据表情符号猜出对应的谚语。

## ✨ 功能特点

- 🤖 **AI 驱动**：使用 Claude API 动态生成谚语和表情符号
- 🎯 **智能匹配**：AI 智能判断答案的正确性（支持近似答案）
- 🚫 **题目不重复**：每个会话中题目不会重复出现
- 💡 **提示系统**：提供 3 次提示机会帮助玩家
- 📊 **计分系统**：实时显示得分和正确率
- 🎨 **精美界面**：渐变色设计，动画效果
- 📱 **响应式**：支持手机和电脑访问

## 🚀 快速开始

### 前置要求

- Node.js 14+
- Anthropic API 密钥

### 安装步骤

1. **克隆或下载项目**

```bash
cd claude-code-sample
```

2. **安装依赖**

```bash
npm install
```

3. **配置环境变量**

创建 `.env` 文件：

```bash
cp .env.example .env
```

编辑 `.env` 文件，添加您的 Anthropic API 密钥：

```env
ANTHROPIC_API_KEY=your_actual_api_key_here
PORT=3000
```

> 💡 在 [Anthropic Console](https://console.anthropic.com/) 获取您的 API 密钥

4. **启动服务器**

```bash
npm start
```

5. **打开游戏**

在浏览器中访问：`http://localhost:3000`

## 🎮 游戏玩法

1. **开始游戏**：页面加载后会自动生成第一个题目
2. **查看表情符号**：仔细观察表情符号的含义
3. **输入答案**：在输入框中输入你猜测的谚语
4. **提交**：点击"提交答案"按钮或按回车键
5. **查看结果**：系统会智能判断答案是否正确
6. **使用提示**：如果遇到困难，可以点击"💡 提示"按钮（共3次机会）
7. **下一题**：点击"下一题"按钮继续游戏

## 🎯 评分规则

- ✅ **答对**：+10 分
- ❌ **答错**：0 分
- 💡 **使用提示**：不扣分，但提示次数有限（每次游戏 3 次）

## 🏗️ 项目结构

```
.
├── emoji-proverb-game.html  # 游戏前端页面
├── server.js                # Express 后端服务器
├── package.json             # 项目依赖配置
├── .env                     # 环境变量（需自行创建）
├── .env.example             # 环境变量示例
├── .gitignore              # Git 忽略文件
└── EMOJI_GAME_README.md    # 本文档
```

## 🔧 技术栈

### 前端
- HTML5
- CSS3（渐变、动画、响应式设计）
- 原生 JavaScript（Fetch API）

### 后端
- Node.js
- Express.js
- Anthropic Claude API

## 📡 API 端点

### POST /api/generate-question
生成新的谚语题目

**请求体：**
```json
{
  "usedProverbs": ["已使用的谚语列表"]
}
```

**响应：**
```json
{
  "proverb": "滴水穿石",
  "emoji": "💧🪨"
}
```

### POST /api/check-answer
检查用户答案是否正确

**请求体：**
```json
{
  "userAnswer": "用户的答案",
  "correctAnswer": "正确答案"
}
```

**响应：**
```json
{
  "isCorrect": true,
  "similarity": 100,
  "explanation": "完全正确"
}
```

### POST /api/get-hint
获取题目提示

**请求体：**
```json
{
  "proverb": "谚语内容"
}
```

**响应：**
```json
{
  "hint": "这是关于坚持不懈的谚语"
}
```

## 🎨 特色功能

### 1. 智能答案判断
使用 Claude AI 判断答案，不仅支持完全匹配，还能识别：
- 近似答案（如"滴水穿石"和"水滴石穿"）
- 同义表达
- 部分正确的答案

### 2. 题目去重机制
- 前端维护已使用谚语列表
- 每次生成新题目时，会避开已使用的谚语
- 确保游戏体验的新鲜感

### 3. 提示系统
- 每个会话提供 3 次提示机会
- AI 生成的提示既有帮助又不直接给出答案
- 提示会包含谚语的主题、寓意或关键信息

### 4. 实时统计
- 当前得分
- 题目编号
- 正确率百分比

## 🔐 环境变量说明

| 变量名 | 必需 | 说明 | 默认值 |
|--------|------|------|--------|
| `ANTHROPIC_API_KEY` | ✅ | Anthropic API 密钥 | 无 |
| `PORT` | ❌ | 服务器端口 | 3000 |

## 🐛 常见问题

### 题目加载失败？
1. 检查 `.env` 文件中的 `ANTHROPIC_API_KEY` 是否正确
2. 确认网络连接正常
3. 查看服务器控制台的错误信息

### API 调用超时？
Claude API 可能需要几秒钟来生成响应，请耐心等待。

### 答案明明是对的却判断为错？
系统使用 AI 进行智能判断，相似度达到 80% 即判定为正确。如果您认为判断有误，可以查看控制台日志了解详细的相似度评分。

## 🚧 未来改进计划

- [ ] 添加难度等级（简单、中等、困难）
- [ ] 支持多人对战模式
- [ ] 添加排行榜功能
- [ ] 支持更多语言的谚语
- [ ] 添加音效和背景音乐
- [ ] 实现题库缓存，减少 API 调用
- [ ] 添加每日挑战模式

## 📝 开发说明

### 本地开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

### API 使用说明

本项目使用 Claude 3.5 Sonnet 模型，主要特点：
- 强大的中文理解能力
- 创意的表情符号生成
- 准确的语义相似度判断

### 成本估算

每个题目大约需要：
- 生成题目：~500 tokens
- 检查答案：~300 tokens
- 获取提示：~200 tokens

平均每题约 1000 tokens，按 Claude API 定价计算成本。

## 📄 许可证

MIT License

## 👨‍💻 作者

Created with Claude Code

---

## 🎉 开始游戏

```bash
npm start
```

然后访问 `http://localhost:3000`，享受游戏的乐趣吧！

如有问题或建议，欢迎提出 Issue！
